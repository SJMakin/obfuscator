name: asmjs-build

on:
  push:
    branches:
      - '**'
  pull_request:

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install deps
        run: npm install

      - name: Build Docker image (Emscripten only - fallback)
        run: npm run docker:build-image-simple

      - name: Build asm.js (simple/no obfuscation)
        run: node scripts/build.mjs simple

      - name: Install Playwright browsers
        run: npx playwright install --with-deps

      - name: Run Playwright tests
        run: npx playwright test

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: dist-simple
          path: |
            dist/asm/module.js
            public/index.html